<form method="post" action="{{ path('app_sale_sale_order_header_done', {'id': saleOrderHeader.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('done' ~ saleOrderHeader.id) }}">
    <button class="btn btn-outline-danger btn-sm"
            data-controller="dom-element"
            data-action="dom-element#confirm"
            data-dom-element-confirm-message-param="Are you sure you are done with this transaction?">
        Close PO
    </button>
</form>
