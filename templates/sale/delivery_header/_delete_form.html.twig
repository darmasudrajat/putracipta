<form method="post" action="{{ path('app_sale_delivery_header_delete', {'id': deliveryHeader.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ deliveryHeader.id) }}">
    <button class="btn btn-danger btn-sm"
            data-controller="dom-element"
            data-action="dom-element#confirm"
            data-dom-element-confirm-message-param="Are you sure you want to delete this item?">
        Cancel
    </button>
</form>
