{
    "subTotal": "{{ saleReturnHeader.subTotal|default|number_format(2) }}",
    "taxNominal": "{{ saleReturnHeader.taxNominal|default|number_format(2) }}",
    "grandTotal": "{{ saleReturnHeader.grandTotal|default|number_format(2) }}",
    "saleReturnDetails": {
        {%- for saleReturnDetailForm in form.saleReturnDetails -%}
            {%- set saleReturnDetail = saleReturnDetailForm.vars.data -%}
            "{{ saleReturnDetailForm.vars.name }}": {
                "unitPrice": "{{ saleReturnDetail.unitPrice|default|number_format(2) }}",
                "total": "{{ saleReturnDetail.total|default|number_format(2) }}"
            }
            {%- if not loop.last -%},{%- endif -%}
        {%- endfor -%}
    }
}
