<form method="post" action="{{ path('app_sale_sale_return_header_delete', {'id': saleReturnHeader.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ saleReturnHeader.id) }}">
    <button class="btn btn-danger btn-sm"
            data-controller="dom-element"
            data-action="dom-element#confirm"
            data-dom-element-confirm-message-param="Are you sure you want to delete this item?">
        Cancel
    </button>
</form>
