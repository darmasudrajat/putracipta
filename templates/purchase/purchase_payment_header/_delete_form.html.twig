<form method="post" action="{{ path('app_purchase_purchase_payment_header_delete', {'id': purchasePaymentHeader.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ purchasePaymentHeader.id) }}">
    <button class="btn btn-danger btn-sm"
            data-controller="dom-element"
            data-action="dom-element#confirm"
            data-dom-element-confirm-message-param="Are you sure you want to delete this item?">
        Cancel
    </button>
</form>
